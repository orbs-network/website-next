# Copy to .env.local and fill in values.

# Contentful Delivery API — required at build time for static export.
CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=

# Optional. Prefixes all routes. In CI this is set to `/<repo-name>` for
# GitHub Pages subpath hosting. Leave blank for local dev.
BASE_PATH=
